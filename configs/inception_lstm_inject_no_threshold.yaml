model:
    name: inception_lstm_inject_no_threshold
    arch_type: cnn_lstm_inject

dataset:
    name: google-cc-100k-no-threshold
    encoding: inception
    is_threshold: False

language:
    max_sentence_length: 40
    embedding_dimension: 300

training:
    batch_size: 40
    epochs: 40

validation:
    batch_size: 40

workspace:
    directory: '/workspace/'